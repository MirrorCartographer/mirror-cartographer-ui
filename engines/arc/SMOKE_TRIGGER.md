# ARC Smoke Trigger

This file intentionally touches `engines/arc/**` so the automatic ARC training smoke benchmark workflow can run from the default-branch workflow definition.

It can be removed later after we confirm the benchmark workflow runs reliably.
