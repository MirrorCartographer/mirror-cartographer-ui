# Lyr ARC-AGI-1 live evaluator

The deployed evaluator endpoint is:

https://mirror-cartographer-ui.vercel.app/api/arc1-eval

It downloads the official ARC-AGI-1 repository at runtime, evaluates all public evaluation tasks with the committed Lyr solver, and returns exact-match evidence as JSON.
