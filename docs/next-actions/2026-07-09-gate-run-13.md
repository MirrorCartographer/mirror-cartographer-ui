# Gate run 13

Decision: block novelty; strengthen live reliability first.

## Implemented

- Commit `17b5e70` updates `tests/live-hosting.spec.js`.
- The live public URL smoke now checks the same audio-start invariant as local smoke.
- No `AudioContext` should be created before the first tap.
- An `AudioContext` should be created only after the sky tap.
- The test still checks a visible sky button, visible canvas, wordless body, and no page or console errors.

## Hosting and testing gate

Vercel remains the primary host for now. The app is small and already has a public URL, so provider migration is not the immediate bottleneck.

Cloudflare Pages remains the best fallback if Vercel build limits or preview reliability keep blocking inspection. Netlify is acceptable but not currently stronger enough to justify churn. GitHub Pages remains a weak primary fit because this project needs preview, live smoke, and rollback workflow more than static-only hosting.

Current test paths:

- `npm run test:phone-contract`
- `npm run test:smoke`
- `npm run test:preview-url`
- `npm run test:live`
- `npm run test:local-gate`
- `npm run test:remote-gate`
- `npm run test:gate`

## Next suggested action

Inspect GitHub Actions after commit `17b5e70`.

If green: create a release tag or stable marker, then start the smallest Composition Clock primitive.

If red: inspect failure artifacts and fix only the failing gate step.
