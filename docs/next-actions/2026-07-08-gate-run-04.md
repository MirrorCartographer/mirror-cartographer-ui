# Gate run 04

Status: reliability gate completed with fallback record.

## Latest inspected state

The previous blocker has been revised in the repository:

- `.github/workflows/smoke.yml` now uses `npm install`, not `npm ci`.
- The workflow runs phone contract, build, and Playwright smoke.
- `package.json` includes `test:phone-contract` and `test:smoke`.
- `playwright.config.js` uses a local Vite preview and a touch-capable mobile Chromium profile.
- GitHub combined status for `main` currently shows Vercel success.

## Decision

Revise the previous action.

The old instruction, “fix npm ci or add lockfile,” is no longer the active blocker because the workflow has already moved to `npm install`.

The new gate is: inspect the smoke workflow result and confirm that both checks pass in GitHub Actions.

## Hosting/testing gate

Vercel remains suitable for the current static Vite/React app. It is still the right primary host because Git push deploys and rollback visibility are enough for this stage.

Do not migrate to Cloudflare Pages, Netlify, GitHub Pages, or a separate stable repo yet.

Cloudflare Pages remains the first parallel-host candidate if Vercel preview/deploy inspection becomes unreliable, but switching hosts before passing smoke would add noise.

GitHub Pages remains weak as primary host because this project depends on rapid iteration, preview quality, and future branch/stable workflow.

A stable branch is useful soon, but only after the smoke workflow proves the app can pass the phone-first contract repeatedly.

## Small implementation attempted

Attempted to add a combined package script:

`test:gate = npm run test:phone-contract && npm run test:smoke`

That direct package update was blocked by write safety checks, so this fallback record was committed instead.

## Pruning verdict

Block novelty.

Do not add Composition Clock, new weather voices, richer music, or visual effects yet.

## Next suggested action

Inspect the GitHub Actions Smoke Test run for `main`.

If it passes:

1. Add the combined `test:gate` script when writes allow it.
2. Create a stable branch or rollback rule.
3. Only then begin the Composition Clock primitive.

If it fails:

1. Fix only the failing check.
2. Preserve phone-first constraints: tap-start audio, no visible explanatory words, low CPU, no crackle, no autoplay violation.
