# Gate run 14

Status: active reliability gate.

## Decision

Block novelty again.

The latest blocker is not a missing live smoke test. The live hosting smoke and live wiring check now exist. The blocker is that the live wiring check is not yet exposed as a first-class package gate.

## Attempted implementation

Attempted to update `package.json` so the remote gate runs the live wiring check before the preview URL check and live Playwright smoke.

The direct package write was blocked by the connector safety layer. No feature code was changed.

## Hosting and testing gate

Keep Vercel as the primary host for now because the live Playwright config already targets the public deployment path.

Do not migrate to Cloudflare Pages or Netlify unless Vercel build availability or rate limits keep preventing live smoke verification.

GitHub Pages remains useful only as a static preview fallback because the app already uses a relative Vite asset base.

Current manual gate order:

1. `npm run test:phone-contract`
2. `npm run build`
3. `npm run test:smoke`
4. `node scripts/live-wiring-check.mjs`
5. `npm run test:preview-url`
6. `npm run test:live`

## Next suggested action

Patch `package.json` with the smallest possible gate wiring change:

- add `test:live-wiring` for `node scripts/live-wiring-check.mjs`;
- run it first inside `test:remote-gate`.

If that write remains blocked, continue with the manual gate order above and fix only the first failing step.

No new music, weather, visual, or interaction feature should be added until the gate is green.
