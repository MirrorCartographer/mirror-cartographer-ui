# Gate run 15

Status: reliability gate advanced.

## Verified before change

- Latest repository commit before this cycle was `813aaa30444e9330a28a85d0d2541fa0c257709a` (`Record gate run 14 decision`).
- That note said the strongest next action was to add a first-class `test:live-wiring` package script and run it first inside `test:remote-gate`.
- `package.json` already exposed `test:live`, `test:preview-url`, `test:local-gate`, `test:remote-gate`, and `test:gate`, but did not expose `test:live-wiring`.
- `scripts/live-wiring-check.mjs` exists and verifies the package script, Playwright live config, concrete live spec file, no stale `LIVE_SITE_URL`, no-autoplay probe, and wordless assertions.
- `playwright.live.config.js` reads `SITE_URL`, defaults to the Vercel app URL, and points at `tests/live-hosting.spec.js`.
- `tests/live-hosting.spec.js` preserves the phone-first deployed smoke contract: visible button/canvas, wordless body before and after tap, zero audio contexts before tap, and audio context creation only after tap.

## Change made

Implemented the smallest package wiring patch at commit `304181eb8f0ce6c99e68fa8c1a3dc28647032396`:

- added `test:live-wiring` as `node scripts/live-wiring-check.mjs`;
- changed `test:remote-gate` to run `test:live-wiring` before preview URL preflight and live Playwright smoke.

## Hosting and testing assessment

- Vercel status for `304181eb8f0ce6c99e68fa8c1a3dc28647032396` reports `success`, so Vercel is currently viable again as the primary preview lane.
- The Vercel status target exposed by GitHub is a dashboard URL, not a public deploy preview URL.
- The known public app hostname `mirror-cartographer-ui.vercel.app` could not be opened through the available web tool because it was not returned by search results, so live browser proof remains incomplete in this run.
- Cloudflare Pages remains the best independent second lane if Vercel rate limits return or if a public URL cannot be fetched/tested.
- Netlify remains a valid static fallback.
- GitHub Pages is feasible only as a lower-priority static fallback because subpath routing/base handling still needs explicit validation.
- The current repo is safe for this reliability patch. Use the existing stable branch/tag strategy before larger creative/audio-visual experiments.

## What remains unverified

- I did not run `npm run test:remote-gate` in a local shell.
- I did not obtain a public deployment URL that could be opened with the browser/screenshot tooling.
- I did not verify the visual/audio runtime on a real hosted page.

## Next suggested action

1. Run `npm run test:live-wiring` or `node scripts/live-wiring-check.mjs` in CI/local shell.
2. Run `SITE_URL=<public-preview-url> npm run test:remote-gate` against the successful Vercel deployment or a Cloudflare/Netlify URL.
3. If remote gate passes, allow the next smallest composition/audio-visual coupling change on a branch.
4. If remote gate fails, patch only the first failing gate.

No new music, weather, visual, or interaction behavior should be added until remote gate proof exists.
