name: Host Bootstrap Contract
on:
  push:
    branches: [main]
    paths:
      - "platform/bootstrap/**"
      - ".github/workflows/bootstrap-contract.yml"
  pull_request:
    paths:
      - "platform/bootstrap/**"
      - ".github/workflows/bootstrap-contract.yml"
permissions:
  contents: read
jobs:
  verify:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "24"
      - run: node platform/bootstrap/verify-bootstrap-contract.mjs
      - run: node platform/bootstrap/test.mjs
